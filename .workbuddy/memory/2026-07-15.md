# 2026-07-15 工作记录

## 排查：导出无图 + "采集失败：没有成功分析的资讯条目"

用户反馈两问题：① 网页"生成完整周报文档"导出的 md 看不到图片；② "新增本期资讯"按钮报
"采集失败：没有成功分析的资讯条目"。

### 根因
1. **导出无图**：`/api/export` 只回纯 md 文本，图片是相对路径 `images/...`（HTML 版用服务器
   绝对路径 `/issues/...`）。单独下载 md/html 时图片不带走、也打不开。本地 `output/weekly-2026-07-14`
   实际有图（43 张），网页内预览（/output/... 路由）正常，纯属导出格式问题。
2. **采集失败**：该错误仅在 LLM 逐条分析**全部抛异常**时抛出。本地实测 ARK Key、抓取（108 条）、
   逐条分析（3/3）均正常 → 说明用户点按钮所在的运行环境（多为部署出去的网页服务）访问不到
   `https://ark.cn-beijing.volces.com`（Key 未注入 / 无外网 / 代理问题 / 速率限制）。

### 修复
- `app/services/export_builder.py`：`_image_src_for_html` 增加 `relative` 参数（离线用相对路径）；
  `_render_overview` 封面去掉 `weekly-YYYY-MM-DD/` 前缀统一为 `images/cover.png`；
  `build_export_html` 加 `relative=False` 参数。
- `web_server.py` `/api/export`：改为返回**自包含 ZIP**（newsletter.md + newsletter.html + images/），
  mimetype=application/zip，文件名 `weekly-{date}.zip`。
- `app/pipeline.py`：`_analyze_parallel` 捕获首个异常；`run_pipeline` 在 candidates 为空时抛出的
  RuntimeError 现在包含真实异常原因 + 排查提示（Key/端点/网络/限流）。

### 验证
- ZIP 导出实测：45 条目（md+html+43 图），md 20 个图片引用全部匹配，封面引用=images/cover.png。
- 三文件 py_compile 通过，调用链无误。

### 待用户确认
- 点击"新增本期资讯"的网页服务运行环境是否注入了 ARK_API_KEY / ARK_BASE_URL / ARK_MODEL 且有外网；
  改后报错信息已能直接显示真实原因。

## 修复：Render 部署"任务已失效"

用户部署在 Render 的 `ai-weekly-report.onrender.com` 点击"新增本期资讯"后，前端轮询任务状态提示"任务已失效"。根因是 `web_server.py` 把任务状态存在内存字典 `TASKS` 里，Render 免费实例在无请求时会休眠/重启，内存清空后任务 ID 丢失。

### 修复
- `web_server.py`：引入 `output/tasks.json` 持久化任务状态；`_new_task` / `_update_task` 均写盘；
  `api_task` 内存未命中时回退读取磁盘；启动时 `_load_tasks()` 恢复历史任务；对"running 超过 30 分钟"的旧任务标记为失败（实例重启导致线程中断）。

### 验证与问题
- `web_server.py` py_compile 通过；`tests/test_newsletter.py` 13/13 通过。
- 首次实现时把 `_load_tasks()` 放在 `TASKS_FILE` 定义之前，导致 Render 部署启动失败（NameError）。已重新排序：常量 → 函数定义 → `_load_tasks()` 调用。
- 本地 `import web_server` 验证初始化成功。

## 修复：期数重复/反向、日期范围

用户反馈"新建成功"后，归档下拉显示：
- 第1期 · 2026年7月2日 — 2026年7月15日（最新一期）
- 第2期 · 2026年7月1日 — 2026年7月14日（旧一期）

最新一期反而被标成"第1期"，旧一期被标成"第2期"，期号反向。

### 根因
1. `app/pipeline.py` 生成周刊时**没有写入 `issue_number`**，字段保持默认 0。
2. `web_server.py` 的 `/api/archive` 在 `issue_number` 缺失时回退到 `(total - idx + 1)`，导致"最旧 = 最后一期、最新 = 第1期"，与直觉相反。
3. `app/services/report_builder.py` 的 `_calc_period` 与 `config.issue_period` 不一致（用 `now - 14 days` 而非 `now - 13 days`），新旧入口生成的日期区间可能差一天。

### 修复
- `app/pipeline.py`：新增 `_calc_issue_number()`，统计已有 `weekly-*/newsletter.json` 与旧 `weekly-*.json` 后 `+1`；在 `run_pipeline` 构造 `WeeklyNewsletter` 时写入 `issue_number`。
- `web_server.py`：归档回退编号改为 `idx`（最旧 = 第1期），并显式处理 `issue_number` 为 0/空的情况。
- `app/services/report_builder.py`：`_calc_issue_number` 与 pipeline 统一；`_calc_period` 改为直接调用 `config.issue_period`，保证日期区间一致。

### 验证
- `py_compile` 三文件通过；`tests/test_newsletter.py` 13/13 通过。
- 本地 Flask test_client 调用 `/api/archive`：旧一期正确显示为"第1期"；`_calc_issue_number` 返回下一期号为 2。
- 部署后，历史 `issue_number=0` 的旧文件会自动被回退编号纠正；新一期由 pipeline 写入正确期号。

## 决策：保留 weekly-2026-07-14

用户原想"全部清空重来"，后改为**保留 2026-07-14 这期**（作为第1期）。已确认的仓库状态：
- 所有修复（ZIP 导出 / 期号 / 任务持久化 / README）已在提交 `b136b32` 中，**本地 HEAD 与 origin/main 一致，已推送 GitHub**。
- `weekly-2026-07-14` 已在 Git 跟踪中（被 .gitignore 的 `!output/weekly-*/` 例外保留），安全。
- 运行时的 `weekly-2026-07-15` 仅在 Render 临时文件系统，**未进 Git**；Render 重新部署即自动清除，无需手动删。
- 用户仅需在 Render Dashboard 点一次 **Manual Deploy → Deploy latest commit**，即可让 7.14 显示为第1期、运行时 7.15 消失。

## 改动：send_md_email.py 动态选期 + md 附件

用户要"发送生成的 md 文件"，但脚本硬编码 `output/weekly-2026-07-13`。改为：
- `_resolve_weekly_dir(date_str=None)`：默认选 `output/weekly-*` 中字典序最新目录；支持命令行 `python send_md_email.py 2026-07-15` 指定日期。
- `prepare_html_and_images` 增加 `images_dir` 参数（不再依赖全局 `IMAGES_DIR`）。
- MIME 改为 `multipart/mixed`：外层保留 HTML 正文 + 内嵌图（原行为），并新增 `newsletter.md` 作为附件。
- 验证：py_compile 通过；`_resolve_weekly_dir()` 默认指向 `weekly-2026-07-14`，md/html/images 均存在。

⚠️ 安全遗留：脚本内 `PASSWORD` 明文硬编码 QQ 邮箱授权码，且 `send_md_email.py` 被 git 跟踪 → 已进 GitHub 历史。需改授权码 + 把密码移出代码（env/配置文件且不提交）+ 清理 git 历史。

## 改动：send_md_email.py 密码改 env + 在线查看链接（方案A）

延续上一条，进一步：
- `PASSWORD` 改为 `os.getenv("QQ_MAIL_PASSWORD", "")`，模块加载时若为空则 `SystemExit` 友好提示去 .env 配置。
- 顶部加 `from dotenv import load_dotenv`，配置区 `os.chdir(BASE_DIR)` 后调用 `load_dotenv()` 读根目录 .env。
- `.env` 追加 `QQ_MAIL_PASSWORD=***`（16位，已泄露，须重新生成授权码）、`WEEKLY_SITE_URL=https://ai-weekly-report.onrender.com/`；`.env` 已被 .gitignore 忽略，不提交。
- 邮件正文末尾追加"📄 在网页查看完整周报"链接（指向根域名，展示最新一期）。用户明确**只要方案A（根域名链接），不要方案B（按 issue 精准链接）**——方案B需前端 `app.js` 支持 `?issue=` 参数，未做。
- 验证：py_compile 通过；导入确认 PASSWORD 长度16、SITE_URL 正确、默认目录=weekly-2026-07-14、footer 链接拼接正确（未实际发信）。

⚠️ 历史泄露：原明文密码仍在 GitHub 公开历史；根本解决 = QQ 邮箱重新生成授权码 + 清理 git 历史。

## 改动：send_md_email.py 支持多收件人

用户问"能否向多邮箱发送"。原 `RECEIVER` 为单值、`sendmail` 只发一个 → 不支持。改为：
- 配置区 `RECEIVER_RAW = os.getenv("QQ_MAIL_RECEIVERS", "lijq@igrslab.com")`，解析为 `RECEIVERS = [r.strip() for r in RECEIVER_RAW.split(",") if r.strip()]`；为空则 SystemExit 提示。
- `main()` 中 `msg["To"] = ", ".join(RECEIVERS)`，`server.sendmail(SENDER, RECEIVERS, ...)`，`print` 输出收件人数与列表。
- `.env` 追加 `QQ_MAIL_RECEIVERS=lijq@igrslab.com`（逗号分隔可扩展多邮箱；`.env` 已被 gitignore，不提交）。
- 验证：py_compile 通过；mock `smtplib.SMTP_SSL` 跑 `main()`，确认 `RECEIVERS=['a@qq.com','b@163.com','c@x.com']` 且 `sendmail` 收到 list、未真发。

注：群发时所有收件人互相可见（To 列出全部）。如需保密可改用 Bcc，用户未要求，未做。

## 整理 README.md 为完整使用手册
- 用户反馈在 VSCode 找不到 README；实际文件一直在 `D:\AI2.0\README.md`（git 跟踪，4.3KB）。多半是 VSCode 打开的不是项目根目录 `D:\AI2.0`。
- 重写 README.md：补全了此前缺失的内容——邮件发送（send_md_email.py）、网页服务（web_app.py / gunicorn web_server:app）、GitHub Actions 自动流（.github/workflows/weekly.yml，仅需配 ARK_API_KEY secret）、本地定时（scheduler.py）、Render 部署步骤、完整 `.env` 变量清单（ARK_* 5 个 + 邮件 3 个 + 代理）、以及今天的踩坑 FAQ（401 端点、任务失效、导出无图、期号反向）。
- 重点强调：`ARK_BASE_URL` 必须是 `/api/coding/v3`（带 coding），否则用 ARK Key 请求标准端点会 401。

## 改动：删除周报"核心趋势/可行性思考"的混乱序号

用户发现生成的 md 末尾"核心趋势"每条带 LLM 乱编的序号（25. 11. 28.…）、可行性思考也带（24. 26. 20.…），要求删除且不再出现。
- 根因：`NumberedParagraph.index` / `NumberedBulletGroup.index`（`daily_report.py`）是 LLM 生成字段，`export_builder.py` 渲染时把 `trend.index` 直接拼到标题前。
- 修复（渲染层剥离，最稳）：`_render_tech_summary`（md）与 `build_export_html`（trends/feasibility）不再输出 `index`，标题改为纯 `**标题**` / `<strong>标题</strong>`。
- 已重新导出 `output/weekly-2026-07-14/newsletter.md` 与 `newsletter.html`（覆盖旧的混乱序号）；经正则校验全局 `^\d+\.\s+\*\*` 残留 = 0。
- `report_reader.py` / 前端 `app.js` 均不渲染 trend/index，故网页预览无此问题。
- 测试 `tests/test_newsletter.py` 中原先假设 `1. **标题**` 格式的 2 个断言（`test_trend_body_indented` / `test_feasibility_bullets_indented`）已同步改为匹配无序号的 `**标题**`；全套 13/13 通过。
- 注：`validate_markdown_structure`（即 `_check_tech_summary_format`）内部仍有 `^\d+\.\s+\*\*` 检查，新格式下该检查不再触发（返回空=通过），属过时但无害，未改。

⚠️ 部署提醒：本次改 `export_builder.py` + 测试，需提交推送 GitHub 并由 Render 重新部署，之后生成的周报才会无序号；本地 7.14 已直接修好。

## 新增：自托管 Linux 部署文档与脚本
用户要在自有 Linux 服务器（VSCode Remote-SSH 已连）部署网页系统。新增文件：
- `DEPLOY.md` — 完整部署指南（代码上服务器 / 建 .env / venv+gunicorn / systemd / nginx 反代 / 数据两种模式 / 排错）。
- `.env.example` — 全部环境变量模板（ARK_* 5 个、QQ 邮件 3 个、AI_WEEKLY_USE_PROXY 代理开关），可提交。
- `deploy/start.sh` — venv+装依赖+gunicorn 启动脚本。
- `deploy/ai-weekly.service` — systemd 守护单元（WorkingDirectory/EnvironmentFile 需按真实路径改）。
关键点：Procfile=`gunicorn web_server:app`、requirements 已含 gunicorn；`.env` 被 gitignore 不会随 clone 下来，须手动建；端点必须是 /api/coding/v3 否则 401。

## 修复：邮件 md 附件序号混乱
用户发现邮件附件的 md 仍有混乱序号（25. / 31. / 10. …）。根因：本地现两期，`weekly-2026-07-14` 此前已重导出干净（0 残留），但 `weekly-2026-07-15` 是用修复前旧代码生成的磁盘文件（6 残留）；邮件脚本 `send_md_email.py` 取最新一期=`dirs[-1]`=7.15，直接附加过期磁盘文件 → 混乱。
- 修复①：`send_md_email.py` 的 md 附件改为**优先用当前（已修复）`build_export_markdown` 实时生成**（读该期 `newsletter.json`），仅在缺 json 时退回磁盘文件 → 邮件 md 永远干净。
- 修复②：把 `weekly-2026-07-14`、`weekly-2026-07-15` 的 `newsletter.md`/`newsletter.html` 用修复后代码重导出覆盖（均已校验 0 残留），网页预览与导出 ZIP 也干净。
- 验证：py_compile OK；`tests/test_newsletter.py` 13/13 通过；邮件附件 md 模拟生成 0 残留。
⚠️ 这些改动（export_builder 修复、重导出文件、send_md_email 改动）尚未 git commit/push；部署到 Linux 前必须提交推送，否则克隆到的仍是旧代码/旧文件。
